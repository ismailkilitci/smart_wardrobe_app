flask==3.0.0
flask-cors==4.0.0
# torch>=2.6 changes default torch.load(weights_only=True) which can break
# older ultralytics checkpoints; keep a known-good pre-2.6 pair.
torch==2.5.1
torchvision==0.20.1
ultralytics>=8.1.0
Pillow==10.1.0
numpy>=1.26.0
